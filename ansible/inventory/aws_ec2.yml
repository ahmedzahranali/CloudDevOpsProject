plugin: amazon.aws.aws_ec2
regions:
  - us-east-1

keyed_groups:
  - key: tags
    prefix: tag

filters:
  instance-state-name: running
  "tag:Name": "DevOps-Project-Jenkins-Server"

compose:
  ansible_host: private_ip_address
